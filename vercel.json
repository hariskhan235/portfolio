{
  "buildCommand": "if ! which flutter; then git clone https://github.com/flutter/flutter.git --depth 1 -b stable /opt/flutter && export PATH=$PATH:/opt/flutter/bin; fi && flutter build web --release",
  "outputDirectory": "build/web",
  "installCommand": "echo 'skip'",
  "framework": null
}